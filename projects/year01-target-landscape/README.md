# Year 1 capstone — public landscape sketch

Default histology: neuroblastoma.

## Do this

1. Keep using the toy matrix until a public summary is cached under `data/raw/` (not committed).
2. Replace paths in `configs/analysis.yaml`.
3. Produce three figures: cohort PCA/UMAP, key-gene boxplots, and a simple oncoprint-style table of recurrent events *if* mutation summaries are available.
4. Write a one-page note with `reports/templates/molecular-note.md`.

## Do not do this

- Do not copy TARGET/OpenPedCan restricted files into git.
- Do not treat the toy matrix as biology; it only teaches the geometry.
