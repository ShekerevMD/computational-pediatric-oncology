# Year 1 programming

Minimum bar:

- Virtualenv + editable install of `cpomp`
- Git commits that a stranger can replay
- One test per function you intend to reuse
- Figures written to `data/processed/figures/` via config, not hardcoded junk paths
