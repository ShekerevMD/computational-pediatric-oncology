# Year 1 intro bioinformatics

Do not start by installing every caller. First understand the *objects*:

reads → alignments → variants / counts → annotated tables → figures that can be wrong.

Practice QC language on the toy matrix (missingness, scaling, outliers) before touching a FASTQ.
When you do run a public pipeline later, wrap it and record versions here.
